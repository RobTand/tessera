# Complete original-unit export intake (#401)

The existing exporter could reuse measured expert blobs but always re-encoded dense tensors. `--cached-units` now closes the existing CachedUnitBundle over every planned dense and expert unit. Dense originals use the shared encoding_input_identity with actual source dtype/bytes, exact Hessian, all activation settings and encoder identity; expert projections retain their existing stronger identity. Missing originals refuse before output creation. Accepted blobs are framed unchanged and recorded by SHA. The legacy expert-only flag retains its previous scope. Complete-cache mode refuses row-sliced dense modules because their original whole wire is not the requested source slice.

The pre-fix PB snapshot `550f2ccca61a75b8ec5d7b186550353fe4667f78dc450c633e7963e84e6a5a57` ran the new end-to-end regression against the original exporter. Its dense-only arm fails with `unrecognized arguments: --cached-units`; the first mixed fixture also exposed an incorrect expert-planner fixture call and was corrected. A subsequent fixture failure came from applying the packaged 7168-row LFM geometry to synthetic 32-row weights; the test now supplies matching miniature output partitions to the same partitioner.

Corrected PB `4224245db613fddc224983ef9f283573c08bb5773d529fa4697da547ced34bd9` passed all 30 cached-producer tests on DL380G10, CPU-only PyTorch 2.11.0, four xdist workers with one native thread each. Zero skips and zero missing collection; no CUDA coverage is claimed. Both dense-only and mixed exports disable the encoder, compare every emitted inner blob with the original, and refuse incomplete dense coverage. Independent terminal/CAS evidence is `/mnt/shared/tessera-clean-runtime-20260907/cached-dense-export-401/red-green-audit.json`.

Wire recipes, producer package sources, runtime pins and serving gates are unchanged. This intake permits an exact measured research reference checkpoint; it does not confer quality, allocation or production promotion.

Additional compatibility checks passed: PB `7fca25148e6f2bf96ffe738f18458c49652b3dcf447ba26b65db525fb9f4f3da` ran explicit-plan, plan-cache and MoE-write tests with four CPU workers: 39 passed, five skipped (`the encoder is a GPU job`), zero missing collection. PB `9fbabdff962d216f315364d15d7e9b24bf2a84eb0f721cdc819cc87aafb431e7` compiled the exporter and both full-reference harness files. `additional-checks.json` beside the first audit rehashes both CAS payloads and confirms zero exits and complete cleanup.
