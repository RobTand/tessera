# GLM q512 repeated-source operator control — 2026-09-07

The actual GLM E288/top8 routed owner passed q256=512: all 864 projection
tiles and channel scales matched the independent producer materialization,
and outputs were bit-exact against stock FP8 for M=1, M=36 covering all 288
slots, and M=4 with input multiplied by 64 to exercise clamp=10.
This extends the [q1024/q768 control](glm_native_20260907.md).

Three trained expert-0 matrices are repeated across all slots. Synthetic
hidden states and controlled routes are used; this is not a trained-router,
expert-permutation, full-model generation, KL, or serving-quality result.
The BF16-source arithmetic screen's relative L2 was 0.443806, 0.438011 and
0.664760 for those three cases. It uses FP32 arithmetic on BF16 source
weights, not a served BF16 baseline. No runtime cell or release gate moved.

The stock vLLM image was
`vllm/vllm-openai@sha256:4e31c581716a5cb9ef31eddb0a425842b75cab07d5cd63fb9572e69ae8794c33`,
with all 4,967 inventoried core files unchanged. The runtime was
`0.28.1rc1.dev451+g1970f3ed4`; the executed backend was TRITON. The consumer
used the control source at `33eb1199` and installed the original producer
package archive identified in the base request. The new optional producer
manifest selects the successful down-projection retry directory explicitly.

Resident owner parameters were 7,257,195,648 bytes; PyTorch's whole-control
peak allocation was 19,169,308,160 bytes, with 21,806,186,496 reserved. This
includes the independent stock oracle and is not an isolated serving peak.
Wire strides were 2,118,415 bytes for w13 and 2,122,464 for w2. No timing or
efficiency claim is made.

The three role actions were submitted through PB's portable campaign
interface; PB placed them on Sparklina, with two CPUs each and native threads
bounded to one. Gate/up passed under an 8 GiB total / 6 GiB GPU reservation.
The down action `c5196ad07c37` ended 137: PB observed 9,511,632,896 GPU bytes,
above its 8 GiB total budget, and stopped the owned scope with
`memory_budget_exceeded`. This was an insufficient reservation, not a system
OOM. Only down was retried, as `cbd28b0edc7f`, with 16 GiB total / 14 GiB GPU;
it passed at 9,441,615,872 PyTorch allocated bytes. The failed attempt remains
in its original directory with no producer receipt; it is not used as input.
All four PB terminal records report complete scope cleanup. Successful CAS
payload hashes were checked against their receipts.

The finite vLLM consumer ran directly under the explicit exemption on
Sparklina from 18:23:45 to 18:25:32 UTC. Launcher and container exited zero,
the container was removed, no OOM occurred, and harness hashes were unchanged.
The separate research selected-expert path is not measured by this receipt.

Evidence root: `/mnt/shared/tessera-glm-native-20260907`.

| Artifact | SHA-256 |
|---|---|
| `q512-audit.json` | `9f48b8c22959fbf5318b88f9cdee7611d517ae00546714e629530e1e432ee366` |
| `control-512-lina-01/receipt.json` | `fae91573c099d56741561db4632f78a4532864688ff2c3d3eb2909a237f325cd` |
| `control-request-q512.json` | `4e745c2c609a8d9b0cd4b79f12185bea64db79579d818fb4be6dad0286c23589` |
| `producer-manifest-q512.json` | `c9fcc8c57694e7b351b575074851b08595fe98e127fb73370990718203f7e611` |
| `construction-02/receipt.json` | `55bde513451a402bcba2feb4a1c10836621dbf0e8e69f6331efe224306ba3fd2` |

The audit binds all consumer/producer files, both down attempts, PB terminal
records and CAS payloads, and the earlier final audit without rewriting any
historical receipt. Exact launch commands are in each `launch.json`.
